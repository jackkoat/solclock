# npm configuration for SolClock
# This file ensures npm works correctly with Vercel deployment

# Use default npm settings (no custom prefix)
# prefix=/home/minimax/.npm-global  # Removed to avoid Vercel conflicts

# Ensure compatibility with Vercel environment
cache=/tmp/.npm